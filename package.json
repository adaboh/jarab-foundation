{
  "name": "jarab-website-v1.1.0",
  "version": "1.0.0",
  "description": "Jarab Foundation Website",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-compile": "sass --watch sass:css -q",
    "devserver": "live-server --port=8091 -q --no-browser ",
    "start": "npm-run-all --parallel devserver sass-compile"
  },
  "author": "Kae",
  "license": "MIT",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "postcss-cli": "^7.1.1"
  }
}
